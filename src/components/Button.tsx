/** @format */

import "./Button.scss";

function Button() {
  return (
    <div class="button">
      <button onClick={() => console.log("Test")}>Test Button</button>
    </div>
  );
}

export default Button;
